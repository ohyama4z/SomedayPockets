# proposal掃除の引き継ぎ

- **日付**: 2026-03-15
- **関連Issue**: #42〜#48（プロセスレビューで起票）

## 今セッションでやったこと
- #30〜#37, #39 を全処理（スキル改善・epic管理定義）
- #40（epic管理定義）完了・マージ済み
- プロセスレビューで #42〜#48 が新たに起票された

## 次セッションでやること

### 即できる（確認不要）

| # | 内容 | 対応方法 |
|---|------|---------|
| #42 | allowed-toolsがランタイム強制されないことが未注記 | `knowledge/` に注意書き追記（1ファイル編集） |
| #43 | gh apiがpermissions.allowに未登録 | `settings.json` に1行追加 |
| #44 | complete-taskでIssue自動クローズ失敗時の対処未定義 | `start-task` にCloses必須明記 + `complete-task` にクローズ確認ステップ追加 |
| #46 | notes/が空のまま形骸化 | `save-progress` SKILL.mdに「特筆すべきこと」の判断基準を具体例で追記 |
| #47 | in-progressラベル除去タイミングがマージ後 | `complete-task` のステップ順変更（ラベル除去をPR Ready前に移動） |
| #48 | turn-counterのN=10がハードコード | `turn-counter.sh` を環境変数 `CLAUDE_REVIEW_INTERVAL` で上書きできるよう修正 |

### 要検討（ユーザーと話す）

| # | 内容 | 論点 |
|---|------|------|
| #45 | session-statusでknowledge/コンテキスト増大 | `INDEX.md` 方式 or 命名規則にタグ追加 or 現状維持か |

## 補足
- settings.jsonはlinterにより整形済み（`Skill(propose)` エントリが追加されている）
- allowed-toolsはランタイム強制バグあり（#42）、ドキュメントとして機能している状態
